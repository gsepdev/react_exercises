import React from "react";
import Alert from "../../components/Alert";
export const Contact= () => {
  return (
    <div>
      <h2>Event listener in a component</h2>
      <Alert />
    </div>
  );
};
